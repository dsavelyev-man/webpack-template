import React from "react";
import { render } from "react-dom";

function App() {
  return <div>
    this is webpack react template
    </div>
}

const root = document.getElementById("root");

render(<App/>, root)